[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
; prefix = asterisk  # REMOVED — Dograh ARI manager builds WS/REST paths from root, doesn't support prefix
enablestatic = yes
sessionlimit = 100

; Enable ARI (REST API for AI integration)
[ari]
  enabled = yes
  pretty = yes
  allowed_origins = *
