FROM python:3.11-slim

LABEL maintainer="AIOS Lab"
LABEL description="AIOS TTS Router — language-aware routing: Urdu→XTTS, English→Chatterbox, fallback→Kokoro"

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY server.py .

EXPOSE 8030

CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8030"]
